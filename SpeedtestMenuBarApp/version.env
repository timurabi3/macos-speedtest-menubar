APP_NAME=SpeedtestMenuBar
# NOTE: bundle id intentionally NOT com.timur.speedtestmenubar — macOS 26
# menu bar management blacklisted that identity (user drag-removal ~2026-07-16)
# and silently suppresses every status item it creates. Fresh id escapes it.
BUNDLE_ID=com.timur.speedtest3
VERSION=5.0.0
BUILD_NUMBER=6
